
# INSTALACION
npm install -y

instala vitest con
npm install -D vitest

en package.json
  "type": "module",

en script agregar :
	"scripts": {
		"test": "vitest",
		"coverage": "vitest run --coverage"
	},

